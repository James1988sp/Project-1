console.log('Js loaded');

const operator = ['+', '-', '/', '*'];
const firstNumber = Math.ceil(Math.random() * 10);
const secondNumber = Math.ceil(Math.random() * 10);
operator = Math.form(Math.random() + operator.length);
